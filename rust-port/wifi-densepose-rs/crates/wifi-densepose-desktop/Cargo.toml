[package]
name = "wifi-densepose-desktop"
version.workspace = true
edition.workspace = true
description = "Tauri v2 desktop frontend for Wave WiFi DensePose"
license.workspace = true
authors.workspace = true

[lib]
name = "wifi_densepose_desktop"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
default = []
# Required by tauri v2 for production (release) builds: serves the bundled
# frontend over the tauri:// custom protocol instead of the dev server.
custom-protocol = ["tauri/custom-protocol"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-stronghold = "2"

# Encryption-at-rest (AES-256-GCM, FIPS 140-3 validated algorithm)
# "prebuilt-nasm" uses pre-assembled .obj files shipped with aws-lc-sys so
# NASM does not need to be installed locally (works on all CI / dev machines).
# To enable the full FIPS 140-3 certification boundary, swap to:
#   aws-lc-rs = { version = "1", features = ["fips"] }
# and install NASM (https://www.nasm.us/), Go (https://go.dev/), CMake first.
aws-lc-rs = { version = "1", features = ["prebuilt-nasm"] }
zeroize = { version = "1.7", features = ["derive"] }

serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
chrono = { version = "0.4", features = ["serde"] }

# Discovery (mDNS + UDP)
mdns-sd = "0.11"
flume = "0.11"

# Serial port (cross-platform)
tokio-serial = "5.4"

# HTTP client for OTA/WASM (native-tls for Windows compatibility)
reqwest = { version = "0.12", default-features = false, features = ["json", "multipart", "native-tls"] }

# Crypto for OTA PSK
sha2 = "0.10"
hmac = "0.12"

# System info for server management
sysinfo = "0.32"

# Async utilities
futures = "0.3"

# Logging
tracing = "0.1"

# UUID for session IDs
uuid = { version = "1.0", features = ["v4", "serde"] }

# Hex encoding for hashes
hex = "0.4"

# OS keyring for SSH key/passphrase storage
keyring = "2"
# Base64 encoding for cloud upload payloads
base64 = "0.22"

# Regex for parsing espflash output
regex = "1.10"

# Serial port for WiFi configuration
serialport.workspace = true

# Auth system — user management, tenancy, RBAC
sqlx = { workspace = true }
argon2 = "0.5"
jsonwebtoken = "9"
rand = "0.8"
machine-uid = "0.5"

# Unix signals for graceful process termination
[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"
